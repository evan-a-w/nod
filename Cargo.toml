[package]
name = "pror-rs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
ocaml = {version = "^1.0.0"}
pror = { git = "https://github.com/evan-a-w/pror", branch = "main" }


[build-dependencies]
ocaml-build = {version = "^1.0.0"}